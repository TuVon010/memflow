# MemFlow Android 开发环境搭建指南

> 原则：全部装 D 盘，VS Code 写代码，真机 USB 调试，不装模拟器

---

## 一、当前已有 vs 需要下载

| 工具 | 状态 | 备注 |
|------|------|------|
| Java 17 | ✅ 已有 | 系统自带 |
| Git 2.52 | ✅ 已有 | 系统自带 |
| **Flutter SDK** | ❌ 需要下载 | ~2.5G → `D:\flutter` |
| **Android Studio** | ❌ 需要下载 | ~5G → `D:\Android\` |
| **VS Code** | ❌ 需要下载 | ~200M → `D:\VSCode` |

D 盘剩余 **64G**，总安装量约 **8G**（不装模拟器省了 3G）。

---

## 二、下载清单（只下载这 3 个）

### ① VS Code

```
地址: https://code.visualstudio.com/download
选: System Installer, 64-bit
安装路径 → D:\VSCode
勾选: ☑ Add to PATH
```

### ② Flutter SDK

```
地址: https://docs.flutter.dev/get-started/install/windows
选: flutter_windows_3.24.x-stable.zip
解压到: D:\flutter
```

### ③ Android Studio

```
地址: https://developer.android.com/studio
选: Windows .exe 安装包
安装时改两个路径:
  - 安装路径 → D:\Android\Android Studio
  - SDK 路径   → D:\Android\SDK
```

---

## 三、一步步安装

### 第1步：装 VS Code

```
双击安装包 → 安装路径填 D:\VSCode → 勾选"添加到PATH" → 完成
```

打开 VS Code，按 `Ctrl+Shift+X` → 搜索安装两个插件：
- **Flutter**（作者 dart.dev）
- **Dart**（作者 dart.dev）

装完重启 VS Code。

### 第2步：装 Android Studio

```
双击安装包 →
  安装路径: D:\Android\Android Studio
  Next →
  SDK 路径:  D:\Android\SDK
  Next → 等待完成
```

首次启动 Android Studio，SDK Setup Wizard 自动弹出，**只需勾选**：

```
☑ Android SDK Platform 34
☑ Android SDK Build-Tools 34.x
☑ Android SDK Platform-Tools
☑ Android SDK Command-line Tools
☑ Google USB Driver                         ← 真机必备！

不勾选:
☐ Android Emulator                          ← 不需要
☐ Intel HAXM                                ← 不需要
☐ 各种 System Image (API 34 等)             ← 不需要
```

> 只选上面 5 项，大小约 2GB（而非完整装的 5GB）。

点击 Finish 等待下载完成，然后关闭 Android Studio——后面基本不用打开了。

### 第3步：解压 Flutter SDK

```
把 flutter_windows_3.24.x-stable.zip 解压到 D:\flutter
确保 D:\flutter\bin\flutter.bat 存在
```

---

## 四、配置环境变量

按 `Win + R` → 输入 `rundll32 sysdm.cpl,EditEnvironmentVariables` → 在**用户变量**区域点"新建"：

```
变量名: FLUTTER_HOME
变量值: D:\flutter

变量名: ANDROID_HOME
变量值: D:\Android\SDK

变量名: PUB_HOSTED_URL
变量值: https://pub.flutter-io.cn

变量名: FLUTTER_STORAGE_BASE_URL
变量值: https://storage.flutter-io.cn
```

然后找到 **Path** 变量 → 双击 → 新建，加两条：

```
D:\flutter\bin
D:\Android\SDK\platform-tools
```

> PUB_HOSTED_URL / FLUTTER_STORAGE_BASE_URL 是 Flutter 国内镜像，否则 `flutter pub get` 极慢。

点确定关闭所有窗口。

---

## 五、验证环境

重新打开一个 PowerShell（让环境变量生效）：

```powershell
flutter doctor -v
```

期望输出：

```
[√] Flutter (Channel stable, 3.24.x, on Microsoft Windows)
    · Flutter version 3.24.x at D:\flutter

[√] Android toolchain - develop for Android devices
    · Android SDK at D:\Android\SDK

[√] Android Studio (version 2024.x)
    · Android Studio at D:\Android\Android Studio

[√] VS Code
    · VS Code at D:\VSCode
```

出现 `[!]` 或 `[X]` 按提示修复：

```powershell
flutter config --android-sdk D:\Android\SDK
flutter doctor --android-licenses   # 一路 y
```

---

## 六、手机开启 USB 调试

这是最关键的一步，每家厂商菜单位置不同：

### 通用步骤

```
1. 设置 → 关于手机 → 连续点击"版本号" 7 次
   → 弹出提示"已开启开发者选项"

2. 设置 → 系统 → 开发者选项 → 打开：
   ☑ USB 调试
   ☑ 安装未知应用（部分手机在"安全和隐私"里）

3. USB 线连接电脑 → 手机会弹窗"允许 USB 调试吗"
   → 勾选"始终允许" → 允许
```

### 各品牌补充

| 品牌 | 额外操作 |
|------|---------|
| **小米** | 还要打开 "USB 安装" 和 "USB 调试(安全设置)" |
| **华为** | 在开发者选项中选 "仅充电模式下允许 ADB 调试" |
| **Oppo/Vivo** | 需登录账号 → 打开 "USB 安装" |
| **三星** | 开发者选项够直接，无需额外操作 |
| **一加** | 够直接 |

### 验证连接

```powershell
flutter devices
```

正确输出：

```
2 connected devices:
SM G9960 (mobile) · 1234567890ABCDEF · android-arm64 · Android 14 (API 34)
```

手机屏幕上会出现 `SM G9960` 这样的型号名，表示连接成功。

---

## 七、启动 MemFlow 项目

```powershell
cd D:\postgraduateLife\redis_critic\memflow

# 安装依赖（首次约 2 分钟）
flutter pub get

# 生成 Isar 模型代码（首次约 1 分钟）
dart run build_runner build --delete-conflicting-outputs

# 直接在手机上运行
flutter run
```

首次运行会编译 APK 并自动安装到手机（约 2-3 分钟）。之后修改代码热重载只需按 `r`。

### VS Code 里操作

1. 打开 `D:\postgraduateLife\redis_critic\memflow` 文件夹
2. 右下角状态栏会显示已连接的手机型号
3. 按 `F5` → 自动编译安装到手机

---

## 八、开发中的日常操作

| 操作 | 方法 |
|------|------|
| 修改代码后立即看到效果 | 在调试控制台输入 `r`（热重载，秒级生效） |
| 改了状态/变量需要重置 | 输入 `R`（热重启，保留 App 数据） |
| App 崩溃/需重新安装 | 输入 `Shift+R` 或直接按 `F5` |
| 查看日志 | 输入 `o`（？= 打开 DevTools） |
| 断点调试 | F9 设断点，F5 运行，VS Code 侧栏查看变量 |

**重要**：热重载 (`r`) 不丢状态，修改 UI 后秒级刷新，不用每次重新编译。

---

## 九、VS Code 快捷键速记

| 操作 | 快捷键 |
|------|--------|
| 运行 | `F5` |
| 热重载 | `r`（调试控制台里输入） |
| 热重启 | `R`（大写） |
| 快速修复 | `Ctrl+.` |
| 格式化 | `Alt+Shift+F` |
| 搜索文件 | `Ctrl+P` |
| 全局搜索 | `Ctrl+Shift+F` |
| 命令面板 | `Ctrl+Shift+P` |

---

## 十、D 盘最终目录结构

```
D:\
├── flutter\              ← Flutter SDK (~2.5G)
│   └── bin\flutter.bat
├── VSCode\               ← VS Code (~200M)
├── Android\
│   ├── Android Studio\   ← (~3G，只用来下载SDK，日常不开)
│   └── SDK\              ← (~2G，无模拟器镜像)
│       ├── platform-tools\
│       └── build-tools\
├── CLion 2023.1.7\       ← 你已有的
├── PyCharm 2024.1.7\     ← 你已有的
└── postgraduateLife\
    └── redis_critic\
        └── memflow\      ← MemFlow 项目
```

---

## 十一、常见问题速查

| 问题 | 解决 |
|------|------|
| `flutter devices` 找不到手机 | 换根 USB 线 / 换电脑接口 / 检查手机上是否弹出授权弹窗 |
| 手机上弹出"未信任的应用" | 手机设置 → 安全 → 允许安装未知来源 |
| `flutter pub get` 超时 | 确认已设 PUB_HOSTED_URL 和 FLUTTER_STORAGE_BASE_URL，重启终端 |
| `build_runner` 报错 | `flutter clean && flutter pub get && dart run build_runner build --delete-conflicting-outputs` |
| 热重载不生效 | 确认在 Debug 模式（不是 Release），控制台输入 `r` 而非 `R` |
| 手机拔掉后 VS Code 断开 | 重新插上 USB → 确认手机上点了"允许" → 按 F5 重新运行 |
| `adb` 命令找不到 | 确认 `D:\Android\SDK\platform-tools` 在 PATH 中 |
| Release APK 太大 | Debug 模式本来就大，正式发布用 `flutter build apk --release` |

---

全部装好后：**手机开 USB 调试 → 插线 → 打开项目 → F5 → 手机上直接看到 MemFlow**。
