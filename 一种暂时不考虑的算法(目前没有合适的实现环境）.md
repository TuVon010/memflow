# MemFlow 完整记忆调度算法 Prompt（终极版）

以下是 MemFlow 应用的完整记忆调度业务逻辑与算法描述，可直接用于 AI 编程辅助（如 ChatGPT、Cursor），无需绑定具体文件结构。此版本加入了**首次流畅奖励长间隔**及**掌握毕业机制**。

---

## 一、每日复习流程（多轮筛查模式）

### 1.1 初始队列生成
每天复习开始时，生成当天的初始复习队列（第一轮），包含：
- **到期卡片**：所有 `due <= today` 的已学卡片，按 `due` 升序排列。
- **新卡片**：从未学习过的卡片（无 `ReviewState` 记录），数量受每日新卡上限限制。
  - 若开启面试倒计时模式，新卡片按优先级降序排列（优先级 = 卡片难度 × 0.6 + 所属卡组权重 × 0.4），否则按创建时间升序。
- 两类卡片按 **3:1 的比例交错**排列（每 3 张复习卡插入 1 张新卡）。

### 1.2 多轮复习循环
1. **第一轮**：用户逐张卡片评分，评分选项为：生疏、犹豫、顺畅。
2. **下一轮队列生成**：本轮结束后，将评分不是“顺畅”的卡片（生疏或犹豫）全部提取，组成下一轮复习队列。
3. 重复步骤 1-2，最多进行 **3 轮**。如果某一轮结束后没有生疏/犹豫卡片，则提前结束当日复习。
4. **重要规则**：
   - 卡片在某一轮被评为“顺畅”后，当天不再出现。
   - 第三轮结束后，无论是否还有未掌握卡片，当日复习结束。
   - 每张卡片在每轮只出现一次。

### 1.3 当日临时数据
- 复习期间，每张卡片在各轮的评分保存在**内存临时结构**中（如 Map<cardId, List<Rating>>），不立即修改持久化的 `ReviewState`。
- 若用户退出复习会话，临时数据丢弃，未完成的卡片状态保持不变。

---

## 二、长期调度算法（SM-2 改良版 + 当日学习画像修正 + 毕业机制）

当日所有轮次结束后，对每张参与复习的卡片执行长期调度更新，写入数据库。

### 2.1 基础 SM-2 算法
单张卡片的调度函数 `scheduleReview(state, rating, interviewMode, cardPriority)`，其中 `rating` 为生疏(0)、犹豫(3)、顺畅(5)。

**参数：**  
- `state.EF`：难度系数，初始 2.5，范围 [1.3, 2.5]  
- `state.interval`：当前间隔天数  
- `state.repetitions`：连续成功次数（评分≥3的次数）  

**更新逻辑：**
```
if rating >= 3:
    newEF = EF + (0.1 - (5 - rating) * (0.08 + (5 - rating) * 0.02))
    repetitions += 1
else:
    newEF = EF - 0.2
    repetitions = 0
EF = max(newEF, 1.3)

if rating == 0:
    newInterval = 1
else:
    if repetitions == 1: newInterval = 1
    else if repetitions == 2: newInterval = 6
    else: newInterval = round(interval * EF)

if interviewMode and cardPriority >= 0.8:
    newInterval = max(1, floor(newInterval * 0.8))

state.due = now + newInterval days
state.lastReview = now
记录评分日志
```

### 2.2 当日学习画像修正因子
为防止仅凭最终评分导致间隔过于乐观/悲观，引入**记忆稳固度评分 `stabilityScore`**，对 SM-2 计算出的基础间隔进行缩放。

**计算 `stabilityScore`（0.0~1.5+）所需数据：** 卡片当天的评分序列 `ratings = [q1, q2, ..., qn]`（0/3/5）。

#### 因子 1：`baseScore`（最终评分基准）
| 最终评分 | baseScore |
|----------|-----------|
| 顺畅(5) | 0.9 |
| 犹豫(3) | 0.6 |
| 生疏(0) | 0.3 |

#### 因子 2：`trendFactor`（学习趋势）
- 只有 1 轮：1.0
- 多轮严格递增（每一轮评分比上一轮高）：1.2
- 出现后轮评分比前轮差：0.8
- 其他持平情况：1.0

#### 因子 3：`lapsePenalty`（生疏次数惩罚）
```
lapseCount = 序列中 0 的次数
lapsePenalty = 1.0 / (1.0 + lapseCount * 0.5)
```

#### 因子 4：`firstAttemptPenalty`（首次非顺畅惩罚）
```
if ratings[0] == 5: 1.0
else if ratings[0] == 3: 0.6
else if ratings[0] == 0: 0.4
```

**稳定性得分公式：**
```
stabilityScore = baseScore × trendFactor × lapsePenalty × firstAttemptPenalty
```

### 2.3 最终间隔计算与硬上限（区分首次流畅度）

计算规则：
1. **首次顺畅奖励**：若当天第一轮评分即为“顺畅”（首次就流畅回忆），且后续没有非顺畅评分（即只有一轮，评分=5），则认为该卡片记忆非常牢固。`stabilityScore` 可能大于 1（例如 baseScore=0.9, trendFactor=1.0, lapsePenalty=1.0, firstAttemptPenalty=1.0，结果为 0.9，但我们可以额外允许乘一个奖励系数）。  
   **改进**：对于首次顺畅且无任何挣扎的卡片，将其稳定性得分乘以一个**流畅奖励因子 `fluencyBonus`**，默认为 1.3，以拉长间隔。

2. **挣扎限制**：若当天出现过任何非顺畅评分（评分0或3），则施加保守上限 `MAX_INTERVAL_AFTER_STRUGGLE`（默认3天），防止过早进入长间隔。即使经过稳定性修正后的间隔较大，也强制限制在此上限内。

3. **正常计算**：其他情况按基础间隔 × stabilityScore 计算，并确保至少为 1 天。

**最终间隔算法：**
```
基础间隔 = SM-2 计算结果（使用最终评分）

if 当天评分序列仅包含一个评分且为 5（首次就顺畅）:
    修正间隔 = round(基础间隔 × stabilityScore × fluencyBonus)
    // 无挣扎，不设硬上限，允许间隔增长至 14-15 天甚至更高
else if 当天出现过非顺畅评分:
    修正间隔 = round(基础间隔 × stabilityScore)
    最终间隔 = min(修正间隔, MAX_INTERVAL_AFTER_STRUGGLE)
else:
    修正间隔 = round(基础间隔 × stabilityScore)
    最终间隔 = 修正间隔

最终间隔 = max(1, 最终间隔)
state.interval = 最终间隔
state.due = today + 最终间隔天
```

**常数：**  
- `MAX_INTERVAL_AFTER_STRUGGLE` = 3 天（可配置）
- `fluencyBonus` = 1.3（可配置，用于奖励首次顺畅的卡片）

### 2.4 卡片毕业（掌握）机制

当一张卡片同时满足以下条件时，标记为**已掌握**，不再主动进入每日复习队列：
- `EF >= 2.5`（难度系数回到初始以上，表示用户认为此卡很容易）
- `repetitions >= 4`（成功复习至少 4 次）
- `interval >= 21` 天（当前间隔达到三周以上）

毕业的卡片不再出现在 `due` 队列中，但用户可在统计或卡片列表中查看。若将来想重新复习，可手动重置状态或降级。

---

## 三、完整当日结束处理流程

1. 遍历本轮参与的所有卡片，收集其完整评分序列。
2. 若为新卡片（首次学习），创建初始 `ReviewState`（EF=2.5, interval=1, reps=0）。
3. 对于每张卡片：
   - 确定最终评分（序列的最后一个值）。
   - 计算 `stabilityScore`。
   - 调用 `SM2_Schedule(state, 最终评分, ...)` 获得基础间隔。
   - 应用流畅奖励或挣扎限制，计算最终间隔和 `due`。
   - 将评分序列追加到 `reviewLog`。
   - 检查毕业条件，若满足则标记 `isGraduated = true`。
   - 持久化 `ReviewState`。
4. 生成当日统计（复习卡片总数、各评分次数、轮次、毕业卡片数等），展示给用户。

---

## 四、特殊场景与边界条件

- **面试倒计时模式**：在队列生成时调整新卡优先级，SM-2 内部缩短间隔，多轮筛查和稳定性计算不变。
- **毕业卡片**：不参与日常 `due` 查询，但若用户手动将其重置或取消毕业，则恢复为普通卡片。
- **用户中断**：若在某一轮中途退出，当日所有临时评分丢弃，已写入数据库的长期状态不受影响。
- **新卡学习**：首次顺畅并享受流畅奖励，间隔可以迅速增长，从而更快达到毕业条件。

---

## 五、实现要求

- 长期调度和稳定性计算独立为纯逻辑函数，不依赖 UI 状态。
- 当日临时队列、评分记录由状态管理模块维护。
- 所有公式中的常数应抽取为可配置变量。
- 代码注释清晰，说明每一步的计算意图，尤其流畅奖励和毕业逻辑。

---

**此 Prompt 覆盖了从当日多轮筛查到长期间隔计算、流畅奖励、硬上限及毕业机制的全链路算法，可直接作为 AI 编程的上下文或实现指令。**