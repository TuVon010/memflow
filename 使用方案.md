要让你的 App 跑在自己手机上，分两种情况：**开发调试** 和 **正式分发**。iOS 和 Android 的操作完全不同，下面分开说明。

---

## 一、Android 安装（简单）

### 1. 开发调试（直接安装）
- **手机**：开启「开发者选项」→ 勾选 **USB 调试**。
- **电脑**：USB 连接手机，Flutter 项目根目录运行：
  ```bash
  flutter run
  ```
- 或者用 `flutter build apk --debug` 生成 APK，传到手机直接安装（需允许「安装未知来源应用」）。

### 2. 分享给别人（非应用商店）
- 执行 `flutter build apk --release`，得到 `build/app/outputs/flutter-apk/app-release.apk`。
- 将此 APK 文件通过网盘、网站、GitHub Release 等方式发布，其他人下载后手动安装即可。
- **无需任何签名证书**（但如果要上架 Google Play，则需要签名）。

---

## 二、iOS 安装（麻烦一些，必须处理签名）

### 1. 开发调试（需要 Apple Developer 账号）
- **免费账号**：只需一个 Apple ID（免费），可以连接真机调试，但每 7 天签名会过期，需重新刷入。
- **步骤**：
  1. Xcode 登录你的 Apple ID（Preferences → Accounts）。
  2. USB 连接 iPhone，在 Xcode 打开 `ios/Runner.xcworkspace`。
  3. 选择你的设备，点击运行。
  4. 首次需要在 iPhone 上信任证书：设置 → 通用 → VPN 与设备管理 → 信任你的开发者证书。
- 也可以让 Flutter 使用 Xcode 的签名：  
  `flutter run`（自动处理签名，使用 Xcode 里的账号）。

### 2. 分发给测试人员（TestFlight，官方推荐）
- **需要**：付费的 Apple Developer Program 账号（$99/年）。
- 用 Xcode 将 App 打包上传到 App Store Connect，然后通过 TestFlight 邀请测试员（最多 10000 人）。
- 测试员安装 TestFlight App，接收邀请即可安装，无签名过期问题。

### 3. 不越狱的侧载方式（有限制）
目前 iOS 侧载方式主要有：
- **AltStore**：利用免费开发者账号签名安装，但每 7 天需在电脑上刷新一次。
- **企业签名**：需要企业开发者账号（$299/年），内部使用，不对外分发。
- **第三方签名服务**：有被封号风险，不推荐。

---

## 三、正式上架应用商店

### Google Play（Android）
1. 注册开发者账号（一次性 $25）。
2. 创建签名密钥（`keytool`）并配置到 `android/key.properties`。
3. 构建正式包：`flutter build appbundle`（或 apk）。
4. 在 Google Play Console 上传 App Bundle，填写信息，提交审核。

### App Store（iOS）
1. 注册 Apple Developer Program（$99/年）。
2. 在 Xcode 中配置 Bundle ID、开发/发布证书、Provisioning Profile。
3. 使用 `flutter build ipa` 导出 .ipa，用 Transporter 上传到 App Store Connect。
4. 完善元数据、截图、隐私标签，提交审核。

---

## 四、对于你这个项目的实操建议

1. **开发阶段**：  
   - 安卓直接用 `flutter run` 到真机最方便。  
   - iOS 推荐用免费 Apple ID 调试，每 7 天重新运行一次，不影响日常开发。

2. **开源发布给用户**：  
   - 安卓：直接在 GitHub Release 放 APK，用户手动安装。  
   - iOS：**无证书的普通用户无法直接安装**。可以考虑：
     - 让有能力的用户自行 Clone 代码，通过免费账号跑在自己手机上（Target 为开发者）。
     - 或者你提供付费开发者账号打包的 TestFlight 链接（仅限测试，不能永久分发）。
     - 长期方案：上架 App Store 是最佳选择，但需付费账号和审核。

3. **如果你只是个人使用或小范围共享**：  
   - 安卓足够，iOS 可以用免费调试方式临时解决。

